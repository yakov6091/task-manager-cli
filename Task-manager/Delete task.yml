info:
  name: Delete task
  type: http

http:
  method: DELETE
  url: http://localhost:4000/tasks/delete/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
