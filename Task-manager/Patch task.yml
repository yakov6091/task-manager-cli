info:
  name: Patch task
  type: http

http:
  method: PATCH
  url: http://localhost:4000/tasks/patch/4
  body:
    type: json
    data: |-
      {
        "title": "no"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
